import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rootie = document.getElementById('root');

const root = ReactDOM.createRoot(rootie);
root.render(<App />);
